apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-academy.fullname" . }}
  labels:
    {{- include "my-academy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-academy.selectorLabels" . | nindent 4 }}

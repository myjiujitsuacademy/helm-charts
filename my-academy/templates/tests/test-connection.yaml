apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "my-academy.fullname" . }}-test-connection"
  labels:
    {{- include "my-academy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Release.Name }}-nextjs-web-server:80']
  restartPolicy: Never
